大功能
	1，添加api格式（目前编程）
	2，模型参数？
	3，多语言？
		至少英文吧？
	4，导入导出？
	5，主题？
	6，增加接口风格：在线使用站点+登录密码，直接模拟用户在官网提供的界面上使用？
	✗ 7，+grok风格：https://docs.x.ai/developers/models
		Grok 完全兼容 OpenAI，不用另做。
	8，+调用摄像头拍照
	✓ 9，+OpenAI 新格式（/v1/responses）
		✓ 可以添加这种端点，并用它发送文字、图片和接收模型回复。
		✓ 参数只在用户明确设置时才发送；也可以继承上级设置，或明确交给模型决定。

小优化
	1，快捷键？
	2，“收起全部”、“发送”、“收展右侧栏”、“发送快捷键弹层触发”这几个按钮图标改为字符（现在是svg）
	3，生图后，在图片上加个“下载”按钮。
	4，生图后，直接保存图片，最好能自动识别图片格式
	5，横向滚动条粗细应跟纵向滚动条保持一致。
bug
	✓ 1，首次进来时，帮助弹窗的关闭按钮不见了。点弹窗之外关闭后再打开则有关闭按钮。
	✓ 2，往上拖动中间的调节输入区高度的横条时，输入区增加的高度应该给输入框，现在是给了“发送”按钮行下方的空间，这不对。
